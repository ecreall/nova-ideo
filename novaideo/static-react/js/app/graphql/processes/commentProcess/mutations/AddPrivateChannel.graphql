mutation AddPrivateChannel($context: String!) {
  addPrivateChannel(context: $context) {
    status
    channel {
      id
      oid
      title
      lenUnreadComments
      subject {
        ... on Person {
          id
          oid
          picture {
            id
            url
            variations
          }
        }
      }
    }
  }
}
