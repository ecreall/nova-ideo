#import "../fragments/Author.graphql"

query Members($first: Int!, $after: String!, $filter: FilterInput!) {
  members(first: $first, after: $after, filter: $filter) {
    totalCount
    pageInfo {
      endCursor
      hasNextPage
    }
    edges {
      node {
        ...Author
      }
    }
  }
}
