#import "../fragments/Idea.graphql"

query IdeasList(
  $first: Int!
  $after: String!
  $filter: FilterInput!
  $generateFilter: Boolean
  $processIds: [String]
  $nodeIds: [String]
  $processTags: [String]
  $actionTags: [String]
) {
  ideas(first: $first, after: $after, filter: $filter, generateFilter: $generateFilter) {
    totalCount
    pageInfo {
      endCursor
      hasNextPage
    }
    edges {
      node {
        ...Idea
      }
    }
  }
}
