#import "../fragments/EntityData.graphql"

query MyFollowings($first: Int!, $after: String!) {
  account {
    id
    followedIdeas(first: $first, after: $after) {
      totalCount
      pageInfo {
        endCursor
        hasNextPage
      }
      edges {
        node {
          ...EntityData
        }
      }
    }
  }
}
