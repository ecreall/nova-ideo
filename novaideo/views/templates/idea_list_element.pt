<a href="${request.resource_url(object, '@@index')}" i18n:translate="">${object.title}</a> modifiée le <span tal:replace="layout.to_localized_time(object.modified_at, date_only=True)"/> à ${object.created_at.hour}:${object.created_at.minute}:${object.created_at.second}
