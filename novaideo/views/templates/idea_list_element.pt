<a href="${request.resource_url(object, '@@index')}" i18n:translate="">${object.title}</a> modifiée le ${object.modified_at.date()} à ${object.created_at.hour}:${object.created_at.minute}:${object.created_at.second}
