<html xmlns:tal="http://xml.zope.org/namespaces/tal"
  tal:omit-tag="">
   <nav class="navbar navbar-default navbar" role="navigation">
     <div class="container">

        <div class="row">
            <div class="col-md-4" tal:condition="actions['menue3']" tal:repeat="(k, a) actions['menue3'].items()">
                <a class="btn btn-default ${((actions['menue3'][k] is None) and 'disabled') or ''} btn-sm" style="width: 300px;" href="${actions['menue3'][k]}" >
                   ${k}
                </a>
            </div>
       </div>
      <!--a class="btn btn-navbar btn-primary nav-control" data-toggle="collapse" data-target=".nav-collapse"-->
          <!--span class="glyphicon glyphicon-chevron-up"></span-->
      <!--/a-->
        <div class="row">
            <div class="col-sm-6 col-md-4">
                ${structure:search_body}
            </div>
            <div class="col-sm-6 col-md-6">
                    <div class="row">
                        <div class="col-md-6">
                          <ul tal:condition="actions['menue1']" tal:repeat="(k, a) actions['menue1'].items()" class="nav nav-pills nav-stacked">
                             <li >
                                <a class="btn btn-primary ${((actions['menue1'][k] is None) and 'disabled') or ''} btn-sm" style="max-width: 300px;" href="${actions['menue1'][k]}" >
                                   ${k}
                                </a>
                             </li>
                          </ul>
                        </div>
                        <div class="col-md-6">
                          <ul tal:condition="actions['menue2']" tal:repeat="(k, a) actions['menue2'].items()" class="nav nav-pills nav-stacked">
                             <li >
                                <a class="btn btn-primary ${((actions['menue2'][k] is None) and 'disabled') or ''} btn-sm" style="max-width: 300px;" href="${actions['menue2'][k]}" >
                                   ${k}
                                </a>
                             </li>
                          </ul>
                      </div>
                 </div>
            </div>
         </div>
      </div>
   </nav>
</html>
<!-- bar -->
