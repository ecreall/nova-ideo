<div class="thumbnail right-caption span4 search-item result-item" 
     i18n:domain="novaideo"
     tal:condition="object.subjects"
     tal:define="
         icon object.icon;
         alert_kind getattr(object, 'alert_kind', '');">
  <div class="media-body" tal:define="subject object.subjects[0]">
    <h4><span class="icon ${icon}"></span> <span tal:replace="layout.to_localized_time(object.modified_at)"/></h4>
    <span tal:condition="alert_kind == 'moderation'">Les modérateurs ont statué sur la contribution <a href="${request.resource_url(subject, '@@index')}"><span class="${getattr(subject, 'icon')}"></span> ${subject.title}</a>.</span>
    <span i18n:translate="" tal:condition="alert_kind == 'new_report'">
      Une nouvelle signalisation vient d'être ajoutée. Les signalisations sont accessibles <a href="${request.resource_url(request.root, '@@seereportedcontents')}">ici</a>.
    </span>
    <span i18n:translate="" tal:condition="alert_kind == 'object_restor'">
      La contribution <a href="${subject.get_url(request)}"><span class="${subject.icon}"></span> ${subject.get_title()}</a> a été restorée.
    </span>
    <span i18n:translate="" tal:condition="alert_kind == 'object_censor'">
      La contribution <a href="${subject.get_url(request)}"><span class="${subject.icon}"></span> ${subject.get_title()}</a> a été censurée.
    </span>
    <span i18n:translate="" tal:condition="alert_kind == 'object_closed'">
      Le groupe de travail sur la proposition <a href="${subject.get_url(request)}"><span class="${subject.icon}"></span> ${subject.get_title()}</a> n'est plus actif depuis quelques cycles de plus d'une semaine. Pour cette raison, le groupe de travail a été dissout et la proposition est maintenant ouverte à un groupe de travail.
    </span>
  </div>
  <span tal:condition="object.is_to_alert(current_user)" style="color: red; margin:5px" class="pull-right glyphicon glyphicon-bell"></span>
</div>