<div tal:condition="amendment is not None" >
    <h3><a tal:attributes="href request.resource_url(amendment, '@@index')">${amendment.title}</a></h3>
    <div class="scroll-able amendment-trimmed-text">
      ${structure:text}
    </div>
    <div class="scroll-able amendment-intention">
        <span tal:condition="is_proposal" style="color: red;">(Texte d'origine)</span>
        <dl  tal:condition="not is_proposal">
        <div tal:omit-tag="" tal:condition="amendment.explanation">
          <dt>Explication</dt>
	  <dd>
              ${structure:amendment.explanation}
          </dd>
        </div> 
        <div tal:omit-tag="" tal:condition="amendment.edited_ideas">
          <dt>Idées modifiées</dt>
	  <dd>
            <tal:loop repeat="idea amendment.edited_ideas">
               <a tal:attributes="href request.resource_url(idea, '@@index')">${idea.title}</a>
               <tal:separator condition="not: repeat['idea'].end">, </tal:separator>
            </tal:loop>
          </dd>
        </div>
        <div tal:omit-tag="" tal:condition="amendment.added_ideas">
          <dt>Idées ajoutées</dt>
	  <dd>
            <tal:loop repeat="idea amendment.added_ideas">
               <a tal:attributes="href request.resource_url(idea, '@@index')">${idea.title}</a>
               <tal:separator condition="not: repeat['idea'].end">, </tal:separator>
            </tal:loop>
          </dd>
        </div>
        <div tal:omit-tag="" tal:condition="amendment.removed_ideas">
          <dt>Idées supprimées</dt>
	  <dd>
            <tal:loop repeat="idea amendment.removed_ideas">
               <a tal:attributes="href request.resource_url(idea, '@@index')">${idea.title}</a>
               <tal:separator condition="not: repeat['idea'].end">, </tal:separator>
            </tal:loop>
          </dd>
        </div>
        </dl>
   </div>
</div>

