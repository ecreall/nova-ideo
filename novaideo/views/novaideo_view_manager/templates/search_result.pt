
${length} elements trouvés

<div class="row">
  <div tal:condition="bodies" id="results">
    <div tal:condition="batch|nothing" metal:use-macro="request.sdiapi.get_macro('pontus:templates/batching.pt', 'batching')"/>
    <div class="col-md-12">
      <div tal:omit-tag="" tal:repeat="body bodies"> ${structure:body}</div>
    </div>
  </div>
</div>

