sudo: required
services:
  - docker
cache:                                                                          
  directories:
    - cache
before_install:
  - docker info
  - docker-compose -v
  - ./run.sh rebuild
script:
  - ./run.sh test -s novaideo
  - ./run.sh test -s dace
  - ./run.sh test -s pontus
notifications:
  email:
    - novaideo-travis@ecreall.com
